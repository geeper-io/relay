apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "relay.fullname" . }}
  labels:
    {{- include "relay.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "relay.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        # Roll the deployment when the ConfigMap or either Secret changes
        checksum/config:     {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret:     {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        checksum/masterkey:  {{ include (print $.Template.BasePath "/secret-master-key.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "relay.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "relay.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}

      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP

          # ── Environment variables ──────────────────────────────────────────
          env:
            # Config file location
            - name: CONFIG_FILE
              value: /app/config/config.yaml

            # ── Master key (dedicated secret) ────────────────────────────────
            - name: PROXY_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.masterKeySecretName" . }}
                  key: PROXY_MASTER_KEY

            # ── API keys (main secret) ───────────────────────────────────────
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: OPENAI_API_KEY
                  optional: true
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: ANTHROPIC_API_KEY
                  optional: true
            - name: AZURE_OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: AZURE_OPENAI_API_KEY
                  optional: true
            - name: AZURE_OPENAI_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: AZURE_OPENAI_ENDPOINT
                  optional: true
            - name: GOOGLE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: GOOGLE_CLIENT_ID
                  optional: true
            - name: GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: GOOGLE_CLIENT_SECRET
                  optional: true
            - name: AUTH_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: AUTH_BASE_URL
                  optional: true
            - name: LANGFUSE_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: LANGFUSE_PUBLIC_KEY
                  optional: true
            - name: LANGFUSE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: LANGFUSE_SECRET_KEY
                  optional: true
            - name: LANGFUSE_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: LANGFUSE_HOST
                  optional: true
            - name: CODE_REVIEW__GITHUB__TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: CODE_REVIEW__GITHUB__TOKEN
                  optional: true
            - name: CODE_REVIEW__GITLAB__TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: CODE_REVIEW__GITLAB__TOKEN
                  optional: true

            # ── Database URL ─────────────────────────────────────────────────
            {{- if .Values.externalDatabase.url }}
            # External DB: DATABASE_URL stored in the proxy Secret
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "relay.secretName" . }}
                  key: DATABASE_URL
            {{- else if .Values.postgresql.enabled }}
            # Bundled PostgreSQL: construct URL using the Bitnami-generated password
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ printf "%s-postgresql" .Release.Name }}
                  key: password
            - name: DATABASE_URL
              value: "postgresql+asyncpg://{{ .Values.postgresql.auth.username }}:$(POSTGRES_PASSWORD)@{{ include "relay.postgresHost" . }}:5432/{{ .Values.postgresql.auth.database }}"
            {{- end }}

            # ── ChromaDB server (when chromadb.server.enabled=true) ──────────
            {{- if .Values.chromadb.server.enabled }}
            - name: CHROMA_HOST
              value: {{ include "relay.fullname" . }}-chromadb
            - name: CHROMA_PORT
              value: {{ .Values.chromadb.server.port | quote }}
            {{- end }}

            # ── Redis URL (when redis subchart is enabled) ────────────────────
            {{- if .Values.redis.enabled }}
            {{- if .Values.redis.auth.enabled }}
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ printf "%s-redis" .Release.Name }}
                  key: redis-password
            - name: RATE_LIMITING__REDIS_URL
              value: "redis://:$(REDIS_PASSWORD)@{{ include "relay.redisHost" . }}:6379"
            {{- else }}
            - name: RATE_LIMITING__REDIS_URL
              value: "redis://{{ include "relay.redisHost" . }}:6379"
            {{- end }}
            {{- end }}

          # ── Probes ─────────────────────────────────────────────────────────
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}

          # ── Resources ──────────────────────────────────────────────────────
          resources:
            {{- toYaml .Values.resources | nindent 12 }}

          # ── Volume mounts ──────────────────────────────────────────────────
          volumeMounts:
            - name: config
              mountPath: /app/config
              readOnly: true
            {{- if and .Values.persistence.chroma.enabled (not .Values.chromadb.server.enabled) }}
            - name: chroma
              mountPath: /app/chroma_data
            {{- end }}
            {{- if .Values.persistence.knowledgeBase.enabled }}
            - name: knowledge-base
              mountPath: /app/knowledge_base
            {{- end }}

      # ── Volumes ─────────────────────────────────────────────────────────────
      volumes:
        - name: config
          configMap:
            name: {{ include "relay.fullname" . }}-config
        {{- if and .Values.persistence.chroma.enabled (not .Values.chromadb.server.enabled) }}
        - name: chroma
          persistentVolumeClaim:
            claimName: {{ include "relay.chromaPvcName" . }}
        {{- end }}
        {{- if .Values.persistence.knowledgeBase.enabled }}
        - name: knowledge-base
          persistentVolumeClaim:
            claimName: {{ include "relay.kbPvcName" . }}
        {{- end }}

      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
